export const environment = {
  // Backend is configured (launchSettings.json) to run at this URL in Development.
  // We use an Angular dev proxy, so from the browser perspective we call relative `/api/...`.
  apiBaseUrl: ''
};

